# GridMind Sentinel — Streamlit Cloud Dashboard Requirements
# Only packages actually imported by the dashboard in Demo Mode.
# Heavy ML packages (faiss, sentence-transformers, chromadb, langgraph)
# are NOT needed for the cloud dashboard — they run locally with the full stack.

# --- Dashboard ---
streamlit>=1.40.0,<1.42.0
streamlit-autorefresh>=1.0.1

# --- Visualization ---
plotly>=5.0.0,<6.0.0
pandas>=2.0.0,<3.0.0

# --- HTTP Client (live mode API calls) ---
httpx>=0.27.0

# --- Config / Env ---
python-dotenv>=1.0.0
pydantic>=2.0.0,<3.0.0
pydantic-settings>=2.0.0,<3.0.0
